
*review_out.json
*.tmp.json

# generated diet artifacts (from dogfooding plan/fix in this repo)
diet-changeset.json
diet-history.json
diet-plan.md

# local dogfood install produced by `token-diet init` run inside this repo
.claude/

# dev-only reading / design docs — never ship in the published repo
dev_only/
CLAUDE.md
